# ============================================================
# DEVELOPMENT — used by `npm run dev`
# ============================================================
# Points to local Express backend on port 5000.
# Override via .env.local if your backend runs on a different
# host or port (e.g., Docker, WSL, remote dev server).
# ============================================================
VITE_API_URL=http://localhost:5000/api

# ============================================================
# PRODUCTION — used by `npm run build`
# ============================================================
# This is the DEFAULT API URL baked into the production build.
# In Vercel, override this via Dashboard → Environment Variables
# to point to your active Cloudflare tunnel URL.
# ============================================================
VITE_API_URL=https://imports-physiology-bite-neutral.trycloudflare.com/api
